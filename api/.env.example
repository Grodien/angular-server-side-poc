# PostgreSQL connection details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=driver_advisory_system
DB_USER=your-db-user
DB_PASSWORD=your-db-password

# API configuration
PORT=3000
# Static API key — set your own value; do not commit real keys.
API_KEY=your-api-key
